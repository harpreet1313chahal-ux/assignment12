import { TableCell } from './TableCell';

const meta = {
  title: 'Components/TableCell',
  component: TableCell,
};

export default meta;

export const Default = {
  args: {
    value: 'John',
  },
};

export const Disabled = {
  args: {
    value: 'Disabled Cell',
  },
};
